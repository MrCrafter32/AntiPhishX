version: '3.8'

services:
  antiphishnlp:
    build: ./NLP
    ports:
      - "3000:3000"
    restart: always

  antiphishx:
    build: ./AntiPhishNextJS
    ports:
      - "80:3000"
    restart: always
    network_mode: host
